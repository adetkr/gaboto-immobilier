<section id="card" class="mt-5">
  <div class="container-fluid bg-white">
      <div>
          <h5 class="text-primary line-height-lg"> <span class="text-secondary"><%= @count %> </span> biens correspondent à votre recherche</h5>

      </div>
    <div class="row">
      <div class="col-12 col-md-6 p-4 my-4 cards">
        <% @flats.each do |flat| %>

            <%= render 'shared/card', flat: flat %>

        <% end %>
      </div>
      <div class="col-12 col-md-6  m-8 p-0">
        <div id="map"
              style="width: 100%; height: 100vh;"
              data-markers="<%= @markers.to_json %>"
              data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>
    </div>
  </div>
</section>

